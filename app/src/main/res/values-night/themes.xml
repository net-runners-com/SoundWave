<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for Jetpack Compose (Dark) -->
    <style name="Theme.SoundWave" parent="android:Theme.Material.NoActionBar">
        <!-- Primary brand color. -->
        <item name="android:colorPrimary">@color/purple_200</item>
        <item name="android:colorPrimaryDark">@color/purple_700</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/purple_700</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>